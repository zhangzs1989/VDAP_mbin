this is a work in progress, but is fairly stable
it takes in a list of quakeml files from swarm and locates the events using the picks and an exhaustive grid search, 
then outputs a catalog and various other QC files.

dependencies: parallel processing toolbox, mapping toolbox, maybe others...
SEPlib executables installed in /opt/, from: 
http://sepwww.stanford.edu/doku.php?id=sep:software:seplib
